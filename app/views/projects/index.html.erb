<h1>INDEX PAGE</h1>
Number of Projects=<%= @count %><br><br>
<h2>Project List</h2>


</br>
<% @projects.each do |project| %>
	<%= project.title %>
	<%= project.abstract %>
	<%= link_to 'Edit', edit_project_path(project.id)%><br>
	<%= link_to 'Delete', project_path(project.id),method:'DELETE'%><br>
<%end%>
<%= link_to 'Add Project', new_project_path %>